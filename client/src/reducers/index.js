import { combineReducers } from 'redux';

export default combineReducers({
  /**
   * TODO: Replace this dummy reducer introduced just so that Redux doesn't complain
   * that it cannot find at least one Reducer.
   * @returns {string}
   */
  replaceMe: () => 'asdf'
});
